ctrl + ~: open Terminal
dir: show folder
cd (change directory) + folder name: go into a folder
cd..: go back to the previous folder
mkdir: create a new folder
print ("something not very interesting in here lol")
python + first letter of the folder name + tab (until the folder is found)
(up): redo the previous step
GIT setup
git init: zip file and send to github acc
git remote add origin + link https from repository on github
git stage .
git commit -m "comment"
git push -u origin master
GIT thereafter
Git add .
Git commit -m "commit"
Git push 